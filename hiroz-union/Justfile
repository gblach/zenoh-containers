repository := "docker.io/gblachmkii/hiroz-union"
platform := "linux/amd64"

build:
	podman build --pull=newer \
		--platform {{ platform }} \
		--tag {{ repository }}:latest \
		--file Dockerfile

push:
	podman push {{ repository }}:latest {{ repository }}:latest
